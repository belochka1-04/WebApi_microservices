name: Build and Push Docker Images

on:
  push:
    branches:
      - main
      - develop

env:
  REGISTRY: docker.io
  DOCKER_USERNAME: belochka104

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - applicationservice
          - jobservice
          - modelnumberservice
          - maskservice
          - partsourceservice
          - documentservice
          - errorlogservice
          - imagejobservice
          - jobdocumentservice
          - jobstockservice
          - modelservice
          - partspicarchiveservice
          - partsservice
          - pricebottasksservice
          - proxyservice
          - replacesarchiveservice
          - responsesservice
          - sourceservice
          - stockcredsservice
          - stopwordsservice
          - uniqmodelservice
          - userservice

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v4
        with:
          context: .
          file: WebApi_${{ matrix.service }}/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:latest
            ${{ env.REGISTRY }}/${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:buildcache,mode=max
